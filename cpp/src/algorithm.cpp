#include "algorithm.hpp"

std::vector<std::pair<int, int>> minimumCostMaximumFlow(const std::vector<Worker>& workers, const std::vector<Mine>& mines) {
	// dummy data so the team can work on their parts
	return { {0, 0}, {1, 1}, {2, 2} };
}